var number =[1,300,20,1,2345];
var i= 0;
var total= 0;

  while(i < number.length){

      console.log(number[i]);
        total = total=number[i];
      i = i+1;
  }
console.log(`total : ${total}`);
